{
    "environment": "development",

    "server" : {
        "host" : "localhost",
        "port" : 8080,
        "db_name" : "DB NAME",
        "db_user" : "DB USER",
        "db_password" : "DB PASSWORD"
    },

    "database": {
        "host": "localhost",
        "port": 5432,
        "name": "rustservice",
        "user": "rustservice",
        "password": "rustservice"
      },

    "logger": {
        "level": "debug"
      }
}